# NoMachine connection details
NOMACHINE_HOST=192.168.1.100
NOMACHINE_USER=myuser
NOMACHINE_PASSWORD=mypassword

# LLM provider: "ollama" or "vllm" (vllm recommended — see README)
LLM_PROVIDER=vllm

# Ollama settings (if LLM_PROVIDER=ollama)
OLLAMA_MODEL=qwen2.5vl:7b

# vLLM settings (if LLM_PROVIDER=vllm)
VLLM_MODEL=openai/yujiepan/ui-tars-1.5-7B-GPTQ-W4A16g128
VLLM_BASE_URL=http://localhost:8080/v1
